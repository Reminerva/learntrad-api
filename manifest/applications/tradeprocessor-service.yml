apiVersion: apps/v1
kind: Deployment
metadata:
  name: tradeprocessor-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tradeprocessor-service
  template:
    metadata:
      labels:
        app: tradeprocessor-service
    spec:
      containers:
        - name: tradeprocessor-service
          image: reksaalamsyah/tradeprocessor-service:latest
          ports:
            - containerPort: 9903
          env:
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                configMapKeyRef:
                  name: tradeprocessor-config
                  key: SPRING_DATASOURCE_USERNAME
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: tradeprocessor-config
                  key: SPRING_DATASOURCE_URL

            - name: SPRING_REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  key: SPRING_REDIS_HOST
                  name: tradeprocessor-config
            - name: SPRING_REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  key: SPRING_REDIS_PORT
                  name: tradeprocessor-config

            - name: SPRING_KAFKA_BOOTSTRAP-SERVERS
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: SPRING_KAFKA_BOOTSTRAP-SERVERS
            - name: LOKI_URL
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: LOKI_URL

            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shared-secrets
                  key: SPRING_DATASOURCE_PASSWORD
            - name: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  key: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
                  name: common-config
                  
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=80.0 -XX:InitialRAMPercentage=40.0 -XX:+UseSerialGC -XX:ReservedCodeCacheSize=96M"
            - name: BPL_JVM_THREAD_COUNT
              value: "20"
            - name: BPL_JVM_HEAD_ROOM
              value: "0"

          # resources:
          #   requests:
          #     memory: "300Mi"
          #     cpu: "25m"
          #   limits:
          #     memory: "400Mi"
          #     cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: tradeprocessor-service
spec:
  selector:
    app: tradeprocessor-service
  ports:
    - protocol: TCP
      port: 9903
      targetPort: 9903
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tradeprocessor-config
data:
  SPRING_DATASOURCE_USERNAME: tradeprocessor_postgres_user
  SPRING_DATASOURCE_URL: jdbc:postgresql://tradeprocessor-postgres.default.svc.cluster.local:5436/tradeprocessor_postgres_db
  SPRING_REDIS_HOST: tradeprocessor-redis.default.svc.cluster.local
  SPRING_REDIS_PORT: "6380"
